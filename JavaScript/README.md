// Textos

alert(“Ola”)
window.prompt(‘Qual seu nome?’) //Exibe um campo pro usuario digitar

window.alert //Exibe apenas o botão OK

document.write //escreve no doc
document.writeIn //escreve e pula linha

console.log

//variáveis

let nome = “Isa” //let pode mudar
ou 
var n = 6

ex: var nome = window.prompt(‘Qual seu nome?’)

const numero = 321328; //const nao muda

//tipos

string
inteiro
boolean(verdadeiro ou falso) //ex: let ativo = true;
array(matriz) //ex: let frutas = [“maçã”, “banana”];
object // ex: let pessoa = { nome: “Isa”, idade = 99};

//concatenação -> usa o “+” ou ${}

window.alert(‘Ola’ + nome)
window.alert(‘Meu nome é ${nome}’)

//soma -> por um tipo para a variável
//formatação
var s //exemplos usando essa variável
var n1 = 2321.5

Number
Number.parseInt
Number.parseFloat 
s.length() //conta qtde de caracteres 
s.toUpperCase() //tudo pra maiuscula
s.toLowerCase() //tudo pra minúscula
n1.toFixed(2) //formatar um número com 2 casas decimais

//operadores
aritmético
+  // soma
-  // subtração
*  // multiplicação
/  // divisão
%  // resto da divisão (módulo)
** // potenciação

atribuição
=   // atribui valor
+=  // soma e atribui
-=  // subtrai e atribui
*=  // multiplica e atribui
/=  // divide e atribui
%=  // resto e atribui

 relacionais
>   // maior que
<   // menor que
>=  // maior ou igual
<=  // menor ou igual
==  // igual (compara apenas valor)
=== // estritamente igual (valor e tipo)
!=  // diferente
!== // estritamente diferente

lógicos
&& // E (AND)
|| // OU (OR)
!  // NÃO (NOT)

ternário
condição ? valorSeVerdadeiro : valorSeFalso 

//DOM
//eventos
https://developer.mozilla.org/pt-BR/docs/Web/API/Event
mouseenter
mousemove
mousedown
mouseup
click
mouseout

//funções



//condições

if(){} else{}
switch(){
case ‘sei la’: skdajs
break
case ‘sei la 2: sdaksld
break
default 
}

// Enquanto
while(){
c++ //nao sei significado esqueci
}

// faça enquanto

do{
} while()

//para

for(inicio; teste; incremento){
} 

